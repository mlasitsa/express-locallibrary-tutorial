extends layout

block content
  h1= title

  ul
    each player in player_list
      li
        a(href=player.url) #{player.first_name} #{player.family_name} (#{player.position})

    else
      li There are no players.
